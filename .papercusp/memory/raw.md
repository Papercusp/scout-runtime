[2026-08-20T13:25:05Z] release-fixer -: A green-checkpoint RED naming ZERO failing tests almost always means the suite never STARTED (install/config load died), not that a test broke — check `npm install --dry-run` and the vitest config load before hunting test code. Root cause here: a standalone repo (no npm workspaces) declared a monorepo-private @papercusp/* dep, so npm install died E404, node_modules never materialized, and one defect surfaced as two unrelated-looking reds (TS2688 + vitest startup error).
